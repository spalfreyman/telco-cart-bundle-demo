{
  "name": "telco-cart-bundle-expander",
  "version": "0.1.0",
  "private": true,
  "description": "commercetools API extension that expands telco bundles into sub-product line items and tags subscription fees for multi-shipping.",
  "main": "src/handler.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node web/server.js",
    "test": "node --test test/*.test.js"
  }
}
